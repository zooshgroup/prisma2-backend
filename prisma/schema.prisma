generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://prismatest:prismatest@127.0.0.1:5432/prismatest?schema=public"
}

model user {
  id       String @id @default(uuid())
  name     String
  password String
}